const recipient = "Freddie";
const salutation = "Hello ";

// Write a single line here.
/*
multiline
is anything
between
these symbols.
*/
let greeting;
if (recipient === "Trump") {
    greeting = "Not you";
} else {
    greeting = salutation + recipient;
}

console.log(greeting);
