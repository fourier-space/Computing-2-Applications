const partialApply = (fn, ...args) => (
    fn.bind()
    // Reasearch the bind function to fill up the inside!
);

const curry;

// Hint: - For the curry function use arrow style and think about
//         how to use ternary expressions and recursion!
//       - The parameters should be (fn, ...args).

export default Object.freeze(partialApply, curry);